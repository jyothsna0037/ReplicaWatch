# ReplicaWatch
Students configure PostgreSQL streaming replication and MongoDB replica sets and develop a monitoring dashboard. The system tracks replication lag, synchronization status, node availability, and failover events. The project demonstrates how database administrators monitor high-availability environments and respond to replication failures. 
